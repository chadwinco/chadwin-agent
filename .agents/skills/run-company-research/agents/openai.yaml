interface:
  display_name: "Run Company Research"
  short_description: "LLM-first report + valuation workflow (no one-shot script)"
  default_prompt: "Use $run-company-research for <TICKER> as of <YYYY-MM-DD>. Work through the LLM research workflow end-to-end (not a single analysis script) and produce report.md plus valuation inputs/outputs."
