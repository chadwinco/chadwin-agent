interface:
  display_name: "Run Company Research"
  short_description: "LLM-first report + valuation workflow (no one-shot script)"
  default_prompt: "Use $run-company-research for <TICKER> as of <YYYY-MM-DD> (or omit ticker to auto-pick from the shared ideas log). Work through the LLM research workflow end-to-end and produce report.md plus valuation inputs/outputs, then remove the completed ticker from the queue."
