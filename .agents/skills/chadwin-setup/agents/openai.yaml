interface:
  display_name: "Chadwin Setup"
  short_description: "Run one-time Python/.venv setup and initialize minimal shared data root artifacts."
  default_prompt: "Use $chadwin-setup to bootstrap Python/.venv setup, environment prerequisites, and minimal shared <DATA_ROOT> artifacts."
